// use std::process::Command;
// use std::env::consts::OS;

// use crate::backend;

// pub fn is_shim_used() -> bool {
//     if ! backend::os::get_os_info().unwrap().contains("Linux") {
//         false
//     } else {
        
//     }
// }